# API Gateway

Standalone IntelliJ/Maven project.

- Port: 8080
- Spring Cloud Gateway Server WebFlux 5.0.1
- Eureka Client 5.0.1
- LoadBalancer 5.0.1

Run: `mvnw.cmd spring-boot:run` (Windows) or run `ApiGatewayApplication`.
